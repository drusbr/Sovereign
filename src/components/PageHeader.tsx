import type { ReactNode } from "react";
export function PageHeader({ eyebrow, title, description, meta, actions }: { eyebrow: string; title: string; description?: string; meta?: string; actions?: ReactNode }) { return <header className="mb-6 flex flex-wrap items-end justify-between gap-4 border-b border-border pb-4"><div><p className="sovereign-kicker">{eyebrow}</p><h1 className="sovereign-title mt-1">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm text-text-muted">{description}</p>}</div><div className="flex items-center gap-4">{meta && <span className="text-xs tabular text-text-muted">{meta}</span>}{actions}</div></header>; }
